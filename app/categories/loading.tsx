import { StorefrontLoading } from "@/components/StorefrontLoading";

export default function Loading() {
  return (
    <StorefrontLoading
      label="Loading categories"
      message="Organising the catalogue so it is easy to browse."
    />
  );
}